'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _xssFilters = require('xss-filters');

var _xssFilters2 = _interopRequireDefault(_xssFilters);

var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var sanitize = function sanitize(attribute) {
    if (!attribute || _lodash2.default.isBoolean(attribute) || _lodash2.default.isNumber(attribute)) {
        return attribute;
    }

    if (_lodash2.default.isObject(attribute)) {
        if (_lodash2.default.isArray(attribute)) {
            _lodash2.default.forEach(attribute, function (value, index) {
                attribute[index] = sanitize(value);
            });
        } else {
            _lodash2.default.forEach(_lodash2.default.keys(attribute), function (key) {
                attribute[key] = sanitize(attribute[key]);
            });
        }

        return attribute;
    }

    return _xssFilters2.default.inHTMLData(attribute);
};

exports.default = {
    sanitize: sanitize
};