'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }(); /**
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      * @author Josh Stuart <joshstuartx@gmail.com>
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      */


var _lodash = require('lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/**
 * An abstract route class
 */

var AbstractRoute = function () {
  function AbstractRoute() {
    _classCallCheck(this, AbstractRoute);
  }

  _createClass(AbstractRoute, [{
    key: 'addPathToRouter',

    /**
     * Adds the route path to the express app router.
     *
     * @param path
     * @param method
     * @param router
     * @param config
     * @param middleware
     */
    value: function addPathToRouter(path, method, router, config, middleware) {
      var _this = this;

      router[method](path, function (req, res, next) {
        // apply target
        _this.setRouteConfigOnResponse(config, req, res);

        next();
      }, middleware);
    }

    /**
     * Sets the model on the request so that other middleware can use it.
     *
     * @param config
     * @param req
     * @param res
     */

  }, {
    key: 'setRouteConfigOnResponse',
    value: function setRouteConfigOnResponse(config, req, res) {
      res.locals.id = config.id;
      res.locals.model = config.model;
      res.locals.populate = config.populate;
      res.locals.mapper = config.mapper;
      res.locals.limit = config.limit;
      res.locals.sanitize = config.sanitize;
      res.locals.search = config.search;
      res.locals.metadata = config.metadata;
      res.locals.lean = _lodash2.default.isUndefined(config.lean) ? true : !!config.lean;
    }
  }]);

  return AbstractRoute;
}();

exports.default = AbstractRoute;